{
  "name": "@repo/db",
  "type": "module",
  "exports": {
    ".": "./src/client.ts"
  },
  "scripts": {
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:seed": "tsx src/seed.ts",
    "db:import": "bun scripts/import-jobs.ts",
    "db:test": "bun scripts/test-db.ts",
    "db:demo": "bun scripts/demo-queries.ts",
    "db:cleanup": "bun scripts/cleanup-db.ts",
    "dev": "tsup src/client.ts --watch",
    "format": "prisma format",
    "generate": "prisma generate",
    "lint": "eslint . --max-warnings 0",
    "prebuild": "npm run generate",
    "studio": "prisma studio"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.2",
    "tsx": "4.19.1",
    "@types/bun": "latest",
    "bun-types": "^1.2.19"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "pg": "^8.16.3",
    "prisma": "^6.13.0"
  }
}
